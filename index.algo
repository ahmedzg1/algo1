ALGORITHM algorithm_name
VAR
    sentence : STRING
BEGIN
    sentence := "car drive"
    write(sentence.length)
    words : INTEGER := 0
    FOR i FROM 1 TO sentence.length STEP 1  DO
        IF (sentence[i]=" ") THEN
            words++
        END_IF
    END_FOR
    write(words)
         count : INTEGER := 0;
        for (var i = 1; i < sentence.length; i++)
          if (str[i] in ["a","u","e","o","y","i"])
            ++count;
         END_FOR
    write(count)
END
